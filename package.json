{
  "name": "inspector-root",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix chairman-web",
    "start": "npm run preview --prefix chairman-web",
    "dev": "npm run dev --prefix chairman-web"
  }
}
