{
  "name": "inspector-root",
  "private": true,
  "workspaces": [
    "chairman-web"
  ],
  "scripts": {
    "build": "npm run build -w chairman-web",
    "start": "npm run preview -w chairman-web -- --host 0.0.0.0 --port ${PORT:-3000}",
    "dev": "npm run dev -w chairman-web"
  }
}
